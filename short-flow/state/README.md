# Persisted short-flow state

`short_flow.sql` is generated by `scripts/short_flow_state.py` in the refresh
workflow. It is the durable, Git-diffable backup of the ignored runtime SQLite
database. GitHub Actions restores it before a refresh and exports it afterward.

This directory is excluded from Vercel deployment by `.vercelignore`.
